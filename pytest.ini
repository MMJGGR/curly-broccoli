[pytest]
pythonpath = api
testpaths =
    api/tests
    compute/tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
    ignore::sqlalchemy.exc.SAWarning
addopts =
    --cov=app
    --cov=compute
    --cov-report=term-missing
    --cov-fail-under=90
